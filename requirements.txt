# 核心依赖
# 运行依赖。测试/代码质量工具见 requirements-dev.txt。

# LLM API客户端
requests>=2.22.0,<3.0.0
openai>=1.12.0,<1.14.0  # DeepSeek兼容OpenAI API，兼容旧pip源的typing-extensions
pydantic>=1.10.0,<2.0.0

# Web记忆管理页
# 当前代码兼容 FastAPI 0.103.x；上限避免旧 pip 源找不到 0.104+。
fastapi>=0.103.0,<0.104.0
uvicorn>=0.22.0,<0.23.0

# 配置管理
PyYAML>=5.1,<7.0

# 日志
structlog>=23.0.0

# 开发工具
python-dotenv>=0.21.0,<0.22.0
